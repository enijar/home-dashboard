import styled from "styled-components";
import vars from "../../config/vars";

export const Wrapper = styled.div`
  padding: ${vars.widgetPadding}em;
  display: flex;
  align-items: center;
  justify-content: center;
`;
